apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name }}
  name: {{ .Release.Name }}
spec:
  ports:
  - name: {{ .Values.port }}-9000
    port: {{ .Values.port }}
    protocol: TCP
    targetPort: 9000
  selector:
    app: {{ .Release.Name }}
  type: ClusterIP
