apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name }}
  name: {{ .Release.Name }}
spec:
  ports:
  - name: "{{ .Values.backend.port }}"
    port: {{ .Values.backend.port }}
    protocol: TCP
    targetPort: 8080
  selector:
    app: {{ .Release.Name }}
  type: ClusterIP
